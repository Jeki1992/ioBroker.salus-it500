# Швидкий старт - Salus IT500 адаптер для ioBroker

## 🚀 Встановлення в ioBroker

### Варіант 1: Встановлення з локальної папки (для розробки)

1. Відкрийте Admin панель ioBroker
2. Перейдіть до розділу "Адаптери"
3. Натисніть на іконку GitHub (встановити з власного URL)
4. Введіть шлях: `file:///Users/oleksandrzaiec/WebstormProjects/iobroker.salus-it500`
5. Натисніть "Встановити"

### Варіант 2: Встановлення через командний рядок

```bash
cd /opt/iobroker
npm install /Users/oleksandrzaiec/WebstormProjects/iobroker.salus-it500
iobroker upload salus-it500
iobroker add salus-it500
```

### Варіант 3: Режим розробки (для тестування)

```bash
cd /Users/oleksandrzaiec/WebstormProjects/iobroker.salus-it500
npm run dev-server
```

Потім відкрийте http://localhost:8081 в браузері

## ⚙️ Налаштування

1. Після встановлення перейдіть до "Екземпляри"
2. Знайдіть "salus-it500" та натисніть на іконку налаштувань
3. Введіть дані:
   - **Username/Email**: ваш email від облікового запису Salus IT500
   - **Password**: ваш пароль
   - **Poll interval**: інтервал опитування в секундах (рекомендовано 60)
4. Збережіть налаштування
5. Адаптер автоматично запуститься

## 📊 Стани після запуску

Після успішного підключення ви побачите наступні стани:

```
salus-it500.0
├── currentTemp        (поточна температура, °C)
├── setTemp           (цільова температура, °C) [можна змінювати]
├── autoMode          (режим AUTO) [можна змінювати]
├── heatStatus        (статус опалення)
└── info
    ├── connection    (статус підключення)
    └── deviceId      (ID пристрою)
```

## 🧪 Швидке тестування

### 1. Перевірка підключення

У Objects перейдіть до `salus-it500.0.info.connection` - має бути `true`

### 2. Встановлення температури

```javascript
// В Scripts створіть новий скрипт:
setState('salus-it500.0.setTemp', 22);
```

### 3. Перемикання режиму

```javascript
// Увімкнути AUTO
setState('salus-it500.0.autoMode', true);

// Вимкнути AUTO
setState('salus-it500.0.autoMode', false);
```

### 4. Читання поточної температури

```javascript
on({ id: 'salus-it500.0.currentTemp', change: 'any' }, function(obj) {
    console.log('Поточна температура: ' + obj.state.val + '°C');
});
```

## 🔍 Перевірка роботи

### Логи адаптера

У розділі "Логи" фільтруйте за "salus-it500":

```
✓ Logged in successfully. Device ID: XXXXX
✓ Status updated - Current: 21.5°C, Set: 22°C, Auto: true, Heat: true
```

### Перевірка через браузер

1. Відкрийте https://salus-it500.com
2. Увійдіть з вашими обліковими даними
3. Порівняйте значення з тими, що показує адаптер

## 🐛 Вирішення проблем

### Адаптер не запускається

```bash
# Перевірте логи
cd /opt/iobroker
iobroker logs --watch salus-it500
```

### Помилка "Login failed"

- Перевірте правильність email та пароля
- Спробуйте увійти на https://salus-it500.com вручну
- Переконайтеся, що облікові дані правильні

### Адаптер запускається, але немає даних

- Перевірте `info.connection` - має бути `true`
- Збільште інтервал опитування до 120 секунд
- Перезапустіть адаптер

### "Token expired" у логах

Це нормально - адаптер автоматично виконає повторний вхід

## 📱 Приклади використання

### Простий сценарій на ніч

```javascript
// О 22:00 знизити температуру
schedule('0 22 * * *', function() {
    setState('salus-it500.0.setTemp', 18);
});

// О 6:00 підвищити температуру
schedule('0 6 * * *', function() {
    setState('salus-it500.0.setTemp', 22);
});
```

### Сповіщення в Telegram

```javascript
// Відправити сповіщення, якщо опалення увімкнулось
on({ id: 'salus-it500.0.heatStatus', val: true, change: 'ne' }, function(obj) {
    sendTo('telegram.0', 'send', {
        text: '🔥 Опалення увімкнулось'
    });
});
```

### Моніторинг температури

```javascript
// Логувати зміни температури
on({ id: 'salus-it500.0.currentTemp', change: 'ne' }, function(obj) {
    log('Температура: ' + obj.state.val + '°C');
});
```

## 📚 Додаткові ресурси

- **README.md** - повна документація
- **EXAMPLES.md** - 12 готових прикладів
- **PROJECT_SUMMARY.md** - технічна інформація

## 🆘 Підтримка

При виникненні проблем:

1. Перевірте логи адаптера
2. Переконайтеся, що версія Node.js >= 20
3. Перевірте, чи працює https://salus-it500.com
4. Створіть issue на GitHub: https://github.com/Jeki1992/ioBroker.salus-it500/issues

## ✅ Чеклист для першого запуску

- [ ] Адаптер встановлено
- [ ] Введено правильні облікові дані
- [ ] Адаптер запущено
- [ ] `info.connection` = true
- [ ] Відображається поточна температура
- [ ] Можна змінити цільову температуру
- [ ] Зміни відображаються на сайті salus-it500.com

---

**Готово!** Тепер ви можете керувати вашим термостатом Salus IT500 через ioBroker! 🎉

